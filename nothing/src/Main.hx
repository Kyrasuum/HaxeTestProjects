package;

/**
 * ...
 * @author Kyrasuum
 */
class Main {
    static public function main() {
        trace("Hello Jaun!");

        trace("is null < 0? " + (null < 0));
        trace("is null < 5? " + (null < 5));
        trace("is null > 5? " + (null > 5));
        trace("is null == 0? " + (null == 0));
    }
}
